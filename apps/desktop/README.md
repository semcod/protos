# Desktop App

Tauri / Electron desktop application.

- Shares the same TypeScript SDK as the web app (`generated/ts/zod/`).
- Communicates with the backend through `gateway/http-api` and `gateway/ws-gateway`.

## Development

```bash
npm install
npm run tauri dev
```

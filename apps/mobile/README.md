# Mobile App

Flutter / React Native mobile client.

- Consumes `generated/dart/` for type-safe Dart model classes.
- Communicates with the backend through `gateway/http-api` (REST) and `gateway/ws-gateway` (WebSocket real-time updates).

## Development

```bash
flutter pub get
flutter run
```
